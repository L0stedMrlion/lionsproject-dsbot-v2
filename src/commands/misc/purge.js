const { EmbedBuilder } = require("discord.js");

module.exports = {
    name: 'purge',
    description: '🧹 Purge all command not older than 14 days!',

    callback: async (client, interaction) => {
    },
};